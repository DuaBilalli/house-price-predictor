# house-price-predictor
A simple Machine Learning project that predicts house prices using Linear Regression. The data is stored in PostgreSQL and accessed with psycopg2, while the model is implemented from scratch in Python without external ML libraries.
